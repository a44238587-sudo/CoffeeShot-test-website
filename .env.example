# Optional backend used for the capture upload path.
# When unset, the page stays usable: it logs the capture and shows a mock AI result.
# Example: https://api.example.com
EXPO_PUBLIC_API_URL=

# Path appended to the base URL. Default: /analyze
EXPO_PUBLIC_ANALYZE_PATH=/analyze
